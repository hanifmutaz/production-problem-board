# Kosongin (default) kalau frontend & backend di-serve dari 1 origin yang sama.
# Isi kalau backend jalan di origin/port lain, mis. pas dev tanpa proxy atau deploy terpisah.
# VITE_API_BASE=http://localhost:3000/api
